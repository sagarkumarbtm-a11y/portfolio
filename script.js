const navToggle=document.querySelector('.nav-toggle');const navLinks=document.querySelector('.nav-links');navToggle?.addEventListener('click',()=>{const open=navLinks.classList.toggle('open');navToggle.setAttribute('aria-expanded',String(open));});document.querySelectorAll('.nav-links a').forEach(link=>link.addEventListener('click',()=>{navLinks.classList.remove('open');navToggle?.setAttribute('aria-expanded','false');}));
const reveals=document.querySelectorAll('.reveal');const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target);}})},{threshold:.16});reveals.forEach(el=>observer.observe(el));
const typing=document.querySelector('.typing-text');if(typing){const words=JSON.parse(typing.dataset.words);let wordIndex=0,charIndex=0,deleting=false;const type=()=>{const word=words[wordIndex];typing.textContent=deleting?word.slice(0,--charIndex):word.slice(0,++charIndex);if(!deleting&&charIndex===word.length){deleting=true;setTimeout(type,1300);return}if(deleting&&charIndex===0){deleting=false;wordIndex=(wordIndex+1)%words.length}setTimeout(type,deleting?45:85)};type();}
document.querySelectorAll('.ripple').forEach(button=>button.addEventListener('click',event=>{const circle=document.createElement('span');const diameter=Math.max(button.clientWidth,button.clientHeight);circle.style.width=circle.style.height=`${diameter}px`;circle.style.left=`${event.clientX-button.getBoundingClientRect().left-diameter/2}px`;circle.style.top=`${event.clientY-button.getBoundingClientRect().top-diameter/2}px`;circle.classList.add('ink');button.appendChild(circle);setTimeout(()=>circle.remove(),600);}));
const form=document.querySelector('.contact-form');form?.addEventListener('submit',event=>{event.preventDefault();const status=form.querySelector('.form-status');const data=new FormData(form);const email=String(data.get('email')||'');const name=String(data.get('name')||'').trim();const message=String(data.get('message')||'').trim();if(!name||!email.includes('@')||message.length<10){status.textContent='Please enter a valid name, email, and message of at least 10 characters.';status.style.color='#b42318';return}status.textContent='Thank you. Your message is ready to send via email.';status.style.color='#006c67';window.location.href=`mailto:sagarkumarbtm@gmail.com?subject=Portfolio enquiry from ${encodeURIComponent(name)}&body=${encodeURIComponent(message)}`;});
